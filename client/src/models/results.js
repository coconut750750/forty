export default class Results {
  constructor(points, defenders, defenseLevel, attackers, attackLevel) {
    this.points = points;
    this.defenders = defenders;
    this.defenseLevel = defenseLevel;
    this.attackers = attackers;
    this.attackLevel = attackLevel;
  }
}